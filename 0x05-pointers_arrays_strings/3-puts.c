#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts - prints a string, followed by a new line, to stdout
 * puts - usage of puts() function
 *
 * @str: string to be printed
 */

void _puts(char *str)
{
	int puts(const char *str)
}
