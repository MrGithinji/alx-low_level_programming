#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * reverse_array - reverses an arry
 *
 * Return: 0
 */

char *string_toupper(char *)
{
	while (*[i])
	{
		_putchar(toupper(str[i]));
		i++;
	}

	return (0);
}
